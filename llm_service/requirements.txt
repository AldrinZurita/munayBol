flask
transformers==4.56.2
tokenizers==0.22.1
numpy<2
datasets
huggingface_hub>=0.23.0
safetensors>=0.4.0
accelerate>=0.31.0
sentencepiece
evaluate
packaging
requests

# Nota: se elimina el pin fijo de torch para evitar reinstalar una wheel CPU-only.
# Instala torch GPU manualmente antes: 
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124